# Система логирования ошибок

## Обзор

Бот теперь имеет продвинутую систему логирования ошибок с автоматическими уведомлениями администраторам.

## Основные возможности

### 1. **Отдельный файл для ошибок**
- Все ошибки (ERROR и CRITICAL) записываются в `logs/error.log`
- Файл НЕ ротируется ежедневно - остается единым
- Формат: `YYYY-MM-DD HH:MM:SS - module_name - LEVEL - message`

### 2. **Автоматические уведомления администраторам**
- При возникновении ошибки автоматически отправляется сообщение всем администраторам
- Уведомления содержат:
  - 📁 Модуль (Nano Banana, Реферальная программа и т.д.)
  - ❌ Краткое описание ошибки
  - 📝 Детали и stack trace (до 500 символов)
  - 🕐 Точное время возникновения

### 3. **Защита от спама**
- Throttling: одинаковые ошибки отправляются не чаще чем раз в 5 минут
- Накопление счетчика: если ошибка повторилась, показывается "(повторилось N раз)"

### 4. **Классификация по модулям**
Ошибки автоматически классифицируются:
- 🍌 **Nano Banana** - nano_banana_service
- 🖼 **DALL-E** - dalle_service
- 🎞 **Veo** - veo_service
- 🎬 **Sora** - sora_service
- 🎵 **Suno** - suno_service
- 👥 **Реферальная программа** - referral
- 💎 **Система подписок** - subscription_service
- 💳 **Платежная система** - payment
- 🗄 **База данных** - database

## Использование

### Автоматическое логирование (рекомендуется)

#### Для сервисов:
```python
from app.core.error_logger import log_service_error

class MyService:
    @log_service_error("Nano Banana")  # Имя сервиса
    async def generate_image(self, prompt: str):
        # Ваш код
        # Любая ошибка будет автоматически залогирована и отправлена админам
        pass
```

#### Для обработчиков:
```python
from app.core.error_logger import log_handler_error

@log_handler_error("Image Generation")  # Имя обработчика
async def process_image(message: Message, state: FSMContext):
    # Ваш код
    # Любая ошибка будет автоматически залогирована
    pass
```

#### С context manager:
```python
from app.core.error_logger import ErrorLogger

async def create_user(user_data):
    with ErrorLogger("Database Operation", "user_creation"):
        # Ваш код
        user = User(**user_data)
        session.add(user)
        await session.commit()
```

### Ручное логирование критических ошибок

```python
from app.core.error_logger import log_critical_error

async def process_payment(payment_id):
    try:
        # ... ваш код ...
        pass
    except Exception as e:
        log_critical_error(
            service_name="Payment System",
            error_message=f"Failed to process payment {payment_id}",
            details={"payment_id": payment_id, "error": str(e)}
        )
        raise
```

## Примеры уведомлений

### Пример уведомления админу:
```
🚨 **Ошибка в боте**

📁 **Модуль:** Nano Banana
❌ **Ошибка:** Failed to generate image: API rate limit exceeded

📝 **Подробности:**
Traceback (most recent call last):
  File "nano_banana_service.py", line 180
    result = await client.generate(...)
google.api_core.exceptions.ResourceExhausted: 429 Too Many Requests
... (truncated)

🕐 **Время:** 2026-01-11 07:30:45

💡 **Действие:** Проверьте логи в logs/error.log
```

### Пример уведомления с повторением:
```
🚨 **Ошибка в боте** (повторилось 15 раз)

📁 **Модуль:** Database
❌ **Ошибка:** Connection timeout

...
```

## Конфигурация

### Список администраторов
Администраторы настраиваются в `.env`:
```env
ADMIN_USER_IDS=123456789,987654321
```

### Период throttling
По умолчанию: 5 минут между уведомлениями об одинаковых ошибках

Изменить можно в `app/core/error_notifier.py`:
```python
self.notification_cooldown = timedelta(minutes=5)  # Измените здесь
```

## Просмотр логов

### В консоли (при локальной разработке):
```bash
tail -f logs/error.log
```

### На сервере:
```bash
# Последние 50 строк
tail -n 50 /opt/bot/logs/error.log

# Следить за новыми ошибками
tail -f /opt/bot/logs/error.log

# Поиск конкретных ошибок
grep "Nano Banana" /opt/bot/logs/error.log

# Ошибки за последний час
grep "$(date -d '1 hour ago' '+%Y-%m-%d %H')" /opt/bot/logs/error.log
```

## Troubleshooting

### Уведомления не приходят?

1. **Проверьте список админов:**
   ```bash
   grep ADMIN_USER_IDS .env
   ```

2. **Проверьте, запущен ли бот:**
   ```bash
   systemctl status bot  # или ps aux | grep python
   ```

3. **Проверьте логи бота:**
   ```bash
   tail -n 100 logs/bot.txt | grep error_notifications
   ```
   Должна быть строка: `error_notifications_enabled`

4. **Проверьте, что ошибка действительно ERROR/CRITICAL уровня:**
   ```python
   logger.error(...)  # ✅ Отправит уведомление
   logger.warning(...)  # ❌ НЕ отправит
   logger.info(...)  # ❌ НЕ отправит
   ```

### error.log слишком большой?

Можно периодически очищать старые записи:
```bash
# Оставить только последние 10000 строк
tail -n 10000 /opt/bot/logs/error.log > /tmp/error_temp.log
mv /tmp/error_temp.log /opt/bot/logs/error.log

# Или создать cron job для автоматической очистки раз в месяц
0 0 1 * * tail -n 10000 /opt/bot/logs/error.log > /tmp/error_temp.log && mv /tmp/error_temp.log /opt/bot/logs/error.log
```

## Best Practices

1. **Используйте декораторы** для автоматического логирования в сервисах
2. **НЕ логируйте** чувствительные данные (пароли, токены, API ключи)
3. **Логируйте контекст**: user_id, operation, parameters (без sensitive данных)
4. **Используйте правильный уровень**:
   - `ERROR` - ошибки, которые нужно исправить
   - `CRITICAL` - критические ошибки, требующие немедленного внимания
   - `WARNING` - предупреждения (не отправляют уведомления)
5. **Добавляйте exc_info=True** для получения stack trace

## Интеграция с новыми модулями

При добавлении нового сервиса:

1. Добавьте mapping в `error_notifier.py`:
```python
module_mapping = {
    "your_service": "Your Service Name",  # Добавьте здесь
    ...
}
```

2. Используйте декоратор в сервисе:
```python
@log_service_error("Your Service Name")
async def your_method(self, ...):
    ...
```

3. Готово! Ошибки будут автоматически классифицироваться.
