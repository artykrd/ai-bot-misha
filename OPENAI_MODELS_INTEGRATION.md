# Интеграция OpenAI моделей

## Обзор

В проект интегрированы следующие модели OpenAI:

### ✅ Реализованные модели

#### 1. **GPT-4 Vision (GPT Image 1)** - Анализ изображений
- **Сервис:** `app/services/ai/vision_service.py`
- **Модель:** `gpt-4o`, `gpt-4-turbo`, `gpt-4.1`
- **Возможности:**
  - Анализ изображений и ответы на вопросы о них
  - Распознавание текста на изображениях
  - Описание содержимого изображений
  - Анализ графиков и диаграмм
  - Поддержка до 10 изображений за запрос
- **Стоимость:** ~1,000 токенов за запрос
- **Доступ:** Меню → 🌄 Создать фото → 👁 GPT Vision
- **Использование:**
  1. Отправьте изображение
  2. Задайте вопрос или дайте инструкцию для анализа

#### 2. **Sora 2** - Генерация видео
- **Сервис:** `app/services/video/sora_service.py`
- **Модели:** `sora-2`, `sora-2-pro`, `sora-2-t2v`, `sora-2-i2v`
- **API Endpoint:** `POST /v1/videos`
- **Возможности:**
  - Генерация видео от 4 до 12 секунд
  - Разрешения: 720p, 1080p
  - Форматы: 16:9, 9:16, 1:1, 4:3, 3:4
- **Стоимость:** ~15,000 токенов за видео
- **Доступ:** Меню → 🎞 Создать видео → ☁️ Sora 2
- **⚠️ Важно:** API требует специального приглашения от OpenAI (не публично доступен)

#### 3. **DALL-E 3** - Генерация изображений
- **Сервис:** `app/services/image/dalle_service.py`
- **Модели:** `dall-e-3`, `dall-e-2`
- **Возможности:**
  - Генерация изображений по текстовому описанию
  - Размеры: 1024x1024, 1792x1024, 1024x1792
  - Качество: standard, hd
  - Стили: vivid, natural
- **Стоимость:** 4,000-8,000 токенов
- **Доступ:** Меню → 🌄 Создать фото → 🖼 DALL-E 3

#### 4. **Whisper** - Расшифровка голоса (Speech-to-Text)
- **Сервис:** `app/services/audio/openai_audio_service.py`
- **Модель:** `whisper-1`
- **API Endpoint:** `POST /v1/audio/transcriptions`
- **Возможности:**
  - Распознавание речи на русском и других языках
  - Поддержка форматов: m4a, mp3, mp4, mpeg, mpga, wav, webm
  - Высокая точность транскрипции
  - Автоматическое определение языка
- **Стоимость:** ~1,000 токенов за минуту аудио
- **Доступ:** Меню → 🎙 Работа с аудио → 🎙 Расшифровка голоса
- **Использование:**
  1. Отправьте голосовое сообщение или аудиофайл
  2. Получите текстовую расшифровку

#### 5. **OpenAI TTS** - Озвучка текста (Text-to-Speech)
- **Сервис:** `app/services/audio/openai_audio_service.py`
- **Модели:** `tts-1`, `tts-1-hd`
- **API Endpoint:** `POST /v1/audio/speech`
- **Возможности:**
  - Конвертация текста в естественную речь
  - 6 доступных голосов:
    - `alloy` - Нейтральный
    - `echo` - Мужской
    - `fable` - Британский акцент
    - `onyx` - Глубокий мужской
    - `nova` - Женский
    - `shimmer` - Мягкий женский
  - Поддержка русского языка
- **Стоимость:** ~200 токенов за запрос
- **Доступ:** Меню → 🎙 Работа с аудио → 🗣 Озвучка текста
- **Использование:**
  1. Отправьте текст для озвучки (до 4096 символов)
  2. Получите MP3 аудио файл

---

## Конфигурация

### Переменные окружения (.env)

```env
# OpenAI API ключ (используется для всех моделей)
OPENAI_API_KEY=sk-...
```

**Важно:** Один ключ OpenAI используется для всех моделей:
- GPT-4 Vision
- Sora 2 (если доступен)
- DALL-E 3
- Whisper
- TTS

---

## Архитектура

### Структура сервисов

```
app/
├── services/
│   ├── ai/
│   │   ├── vision_service.py       # GPT-4 Vision (новый)
│   │   ├── openai_service.py       # GPT текстовые модели
│   │   └── __init__.py             # Экспорты
│   ├── audio/
│   │   ├── openai_audio_service.py # Whisper + TTS
│   │   └── __init__.py
│   ├── image/
│   │   ├── dalle_service.py        # DALL-E 3
│   │   └── __init__.py
│   └── video/
│       ├── sora_service.py         # Sora 2 (обновлен)
│       └── __init__.py
└── bot/
    ├── handlers/
    │   └── media_handler.py        # Обработчики для всех моделей
    └── keyboards/
        └── inline.py               # Меню и кнопки
```

### Новые обработчики

**В `media_handler.py` добавлены:**

1. **FSM States:**
   - `waiting_for_whisper_audio` - Ожидание аудио для Whisper
   - `waiting_for_vision_image` - Ожидание изображения для Vision
   - `waiting_for_vision_prompt` - Ожидание вопроса для анализа

2. **Callback handlers:**
   - `start_whisper()` - Запуск Whisper расшифровки
   - `start_tts()` - Запуск TTS озвучки
   - `start_gpt_vision()` - Запуск GPT Vision анализа

3. **Message handlers:**
   - `process_whisper_audio()` - Обработка аудио для расшифровки
   - `process_tts_audio()` - Обработка текста для озвучки
   - `process_vision_image()` - Получение изображения
   - `process_vision_prompt()` - Анализ изображения

---

## Использование API ключа

Все модели OpenAI используют **единый API ключ** из переменной `OPENAI_API_KEY`:

```python
from app.core.config import settings

# Все сервисы получают ключ автоматически
vision_service = VisionService()  # Использует settings.openai_api_key
whisper_service = OpenAIAudioService()  # Использует settings.openai_api_key
dalle_service = DalleService()  # Использует settings.openai_api_key
sora_service = SoraService()  # Использует settings.openai_api_key
```

---

## Стоимость токенов

| Модель | Стоимость (токены) | Описание |
|--------|-------------------|----------|
| **GPT-4 Vision** | ~1,000 | За анализ изображения |
| **Sora 2** | ~15,000 | За видео (4-12 сек) |
| **DALL-E 3 (standard)** | ~4,000 | За изображение |
| **DALL-E 3 (hd)** | ~8,000 | За HD изображение |
| **Whisper** | ~1,000 | За минуту аудио |
| **TTS** | ~200 | За озвучку текста |

---

## Тестирование

### Проверка интеграции

1. **GPT Vision:**
   ```
   /start → 🌄 Создать фото → 👁 GPT Vision
   - Отправьте изображение
   - Напишите: "Что на этом изображении?"
   ```

2. **Whisper:**
   ```
   /start → 🎙 Работа с аудио → 🎙 Расшифровка голоса
   - Отправьте голосовое сообщение
   ```

3. **TTS:**
   ```
   /start → 🎙 Работа с аудио → 🗣 Озвучка текста
   - Отправьте текст: "Привет, это тест озвучки"
   ```

4. **DALL-E 3:**
   ```
   /start → 🌄 Создать фото → 🖼 DALL-E 3
   - Отправьте: "Красивый закат на море"
   ```

5. **Sora 2:**
   ```
   /start → 🎞 Создать видео → ☁️ Sora 2
   - Отправьте: "Золотой ретривер играет в поле"
   - ⚠️ Требуется доступ к API
   ```

---

## Примечания

### Sora 2 API
- **Статус:** Не публично доступен (требуется приглашение)
- **Альтернативы:** Используйте Veo 3.1, Luma, или другие доступные сервисы
- Код готов к использованию, когда API станет доступен

### Лимиты
- **Vision:** До 10 изображений за запрос
- **TTS:** До 4096 символов текста
- **Whisper:** Поддерживаются основные аудио форматы

---

## Поддержка

Если возникли вопросы:
- Проверьте, что `OPENAI_API_KEY` установлен в `.env`
- Убедитесь, что у аккаунта OpenAI есть кредиты
- Для Sora 2 проверьте доступ к API (требуется приглашение)

---

**Дата интеграции:** 18 ноября 2025
**Версия:** 1.0
