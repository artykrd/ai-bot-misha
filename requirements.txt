# Core
python-dotenv==1.2.2  # CVE-2026-28684
pydantic==2.5.0
pydantic-settings==2.1.0

# Telegram Bot
# Bumped from 3.4.1 to a version that allows aiohttp>=3.13 (closes the bulk
# of CVE-2025/2026 in aiohttp). Public Dispatcher/Router/Filter/FSM APIs
# are unchanged from 3.4 onwards.
aiogram==3.27.0

# FastAPI
fastapi==0.118.0  # PYSEC-2024-38, brings starlette>=0.47 (CVE-2024-47874, CVE-2025-54121, CVE-2025-62727)
uvicorn[standard]==0.27.0
python-multipart==0.0.27  # PYSEC-2024-38, CVE-2024-53981, CVE-2026-24486/40347/42561

# Database
SQLAlchemy==2.0.25
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Redis
redis==5.0.1
hiredis==2.3.2

# HTTP Client
# httpx>=0.28.1 is required by google-genai>=1.3.0 (Veo). Verified compatible
# with openai==1.10.0 / anthropic==0.40.0 (both allow httpx<1).
httpx==0.28.1
aiohttp==3.13.4  # PYSEC-2024-24/26, CVE-2024-27306/30251/52304, CVE-2025-53643/69223-69230/69225-69227, CVE-2026-22815/34513-34520/34525

# AI APIs
# openai/anthropic bumped to httpx-0.28-compatible releases. Older versions
# (openai 1.10 / anthropic 0.40) always passed `proxies=` to httpx.AsyncClient,
# which httpx>=0.28 removed -> "unexpected keyword argument 'proxies'" broke every
# text/image/audio model. httpx 0.28 is required by google-genai (Veo). Both
# still pin pydantic<2.13 (aiogram-compatible). Our API surface (chat.completions,
# images.generate/edit/create_variation, audio.speech/transcriptions, messages)
# is unchanged across these versions.
openai==1.109.1
anthropic==0.111.0
# Two Google SDKs: the legacy `google.generativeai` (text/vision) and the new
# `google.genai` Client (Imagen/Veo via API key). The code falls back between them.
# google-genai>=1.3.0 is required — generate_videos was added in that release.
# Pinned to 1.3.0: it is the minimum with generate_videos AND still works with
# pydantic==2.5.0 / aiogram (pydantic<2.13). Newer google-genai (>=1.37) pulls
# pydantic>=2.13 which conflicts with aiogram 3.27 — do not bump without also
# upgrading aiogram and re-testing pydantic models.
google-generativeai>=0.8.0,<1.0
google-genai==1.3.0
PyJWT==2.12.0  # CVE-2026-32597 fix

# Payment
yookassa==3.0.0

# Background Tasks
APScheduler==3.10.4

# File Processing
Pillow==12.2.0  # CVE-2024-28219, CVE-2026-25990/40192/42308/42309/42310/42311
python-magic==0.4.27

# Logging
structlog==24.1.0
python-json-logger==2.0.7

# Utils
phonenumbers==8.13.27
pytz==2023.3

# Monitoring
psutil==5.9.8

# Testing
pytest==9.0.3  # CVE-2025-71176
pytest-asyncio==0.23.3
pytest-cov==4.1.0
# httpx is already declared above (==0.28.1); kept here for the test extra group.
faker==22.0.0
