
# Руководство по тестированию бота

## Быстрый старт

### 1. Запуск бота в тестовом режиме (без API ключей)

```bash
# Убедитесь что PostgreSQL и Redis запущены
systemctl start postgresql redis-server

# Запустите бота
python main.py
```

Бот работает в **mock режиме** - все AI запросы обрабатываются тестовым сервисом без реальных вызовов к API.

### 2. Тестирование в Telegram

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Вы увидите главное меню с кнопками

## Сценарии тестирования

### Сценарий 1: Выбор модели и текстовый запрос

**Шаги:**

1. `/start` → Главное меню
2. Нажать "🤖 Выбрать модель"
3. Выбрать любую модель (например, GPT-4)
4. Отправить текстовое сообщение: "Привет, как дела?"
5. Получить ответ

**Ожидаемый результат:**
- Бот отвечает mock-ответом с пометкой "⚠️ Используется тестовый режим"
- Токены списываются с баланса
- Ответ содержит фрагмент вашего запроса

**Проверка логов:**
```bash
# В логах должна быть запись:
{"event": "ai_request_completed", "model": "gpt-4", "is_mock": true}
```

### Сценарий 2: Тестирование разных моделей

**Модели для тестирования:**

| Модель | Callback | Стоимость | Проверка |
|--------|----------|-----------|----------|
| GPT-4 Omni | `model:gpt-4` | 1,000 | Ответ содержит [Mock GPT-4 Response] |
| GPT-4 Mini | `model:gpt-4-mini` | 250 | Ответ содержит [Mock GPT-4 Mini Response] |
| Claude 3.5 | `model:claude` | 1,200 | Ответ содержит [Mock Claude Response] |
| Gemini Pro | `model:gemini` | 900 | Ответ содержит [Mock Gemini Response] |
| DeepSeek | `model:deepseek` | 800 | Ответ содержит [Mock DeepSeek Response] |

**Проверка:**
```python
# Каждая модель должна давать уникальный ответ
pytest tests/test_bot_integration.py::TestAIModelSelection::test_mock_service_different_models -v
```

### Сценарий 3: Проверка профиля

**Шаги:**

1. `/start` → Главное меню
2. Нажать "👤 Мой профиль"
3. Проверить отображение данных

**Ожидаемый результат:**
- Отображается Telegram ID
- Отображается имя пользователя
- Отображается баланс токенов
- Отображается статус подписки
- Нет ошибок Markdown parsing

**Предыдущая ошибка (исправлена):**
```
TelegramBadRequest: can't parse entities: Can't find end of the entity starting at byte offset 88
```

### Сценарий 4: Проверка подписки

**Шаги:**

1. `/start` → Главное меню
2. Нажать "💎 Подписка"
3. Просмотреть тарифы

**Ожидаемый результат:**
- Отображаются все тарифы
- Кнопки "Купить" работают (создание платежа пока не реализовано)
- Кнопка "Активировать промокод" работает

### Сценарий 5: Админ-панель

**Шаги:**

1. Запустить админ-бота: `python admin_main.py`
2. Отправить `/start` админскому боту
3. Попробовать команды:
   - `/stats` - статистика
   - `/users` - список пользователей

**Ожидаемый результат:**
- Нет deprecation warnings
- Статистика отображается корректно
- Пользователи отображаются

**Предыдущий warning (исправлен):**
```
DeprecationWarning: Passing parse_mode to Bot initializer is deprecated
```

### Сценарий 6: Проверка токенов

**Шаги:**

1. Отправить несколько AI запросов
2. Проверить баланс через "👤 Мой профиль"
3. Убедиться что токены списываются

**Проверка в БД:**
```sql
SELECT
    u.telegram_id,
    u.full_name,
    SUM(s.tokens_remaining) as total_tokens
FROM users u
LEFT JOIN subscriptions s ON s.user_id = u.id AND s.is_active = true
WHERE u.telegram_id = YOUR_TELEGRAM_ID
GROUP BY u.id;
```

## Автоматические тесты

### Запуск всех тестов

```bash
# Все тесты
pytest tests/ -v

# Только интеграционные тесты
pytest tests/test_bot_integration.py -v

# Только тесты AI
pytest tests/test_bot_integration.py::TestAIModelSelection -v

# С покрытием
pytest tests/ --cov=app --cov-report=html
```

### Тесты AI сервисов

```bash
# Тест что factory возвращает mock без API ключей
pytest tests/test_bot_integration.py::TestAIModelSelection::test_ai_factory_returns_mock_without_api_keys -v

# Тест поддержки всех моделей
pytest tests/test_bot_integration.py::TestAIModelSelection::test_ai_factory_supports_all_models -v

# Тест генерации текста
pytest tests/test_bot_integration.py::TestAIModelSelection::test_mock_service_generates_text -v
```

### Тесты хендлеров

```bash
# Тест кнопки выбора модели
pytest tests/test_bot_integration.py::TestBotHandlers::test_select_model_button -v

# Тест выбора конкретной модели
pytest tests/test_bot_integration.py::TestBotHandlers::test_choose_model_button -v
```

## Проверка исправленных багов

### Bug #1: Markdown Parsing Error ✅

**Было:**
```
TelegramBadRequest: can't parse entities: Can't find end of the entity starting at byte offset 88
```

**Исправление:**
- Разделение логики форматирования username
- Явное указание `parse_mode="Markdown"`

**Тест:**
```bash
# Нажать "Мой профиль" - ошибки не должно быть
```

### Bug #2: Deprecation Warning ✅

**Было:**
```python
admin_bot = Bot(token=..., parse_mode="Markdown")  # Deprecated
```

**Исправление:**
```python
from aiogram.client.default import DefaultBotProperties
from aiogram.enums import ParseMode

admin_bot = Bot(
    token=...,
    default=DefaultBotProperties(parse_mode=ParseMode.MARKDOWN)
)
```

**Тест:**
```bash
python admin_main.py  # Не должно быть warnings
```

### Bug #3: Dispatcher Storage Initialization ✅

**Было:**
```python
dp = Dispatcher(storage=MemoryStorage())  # Module level
# Later: dp.storage = RedisStorage(...)  # Error: no setter
```

**Исправление:**
```python
async def setup_bot() -> Dispatcher:
    redis_storage = RedisStorage(redis=redis_client.fsm_client)
    dp = Dispatcher(storage=redis_storage)
    return dp
```

**Тест:**
```bash
python main.py  # Бот должен запуститься без ошибок
```

## Нагрузочное тестирование

### Параллельные запросы

```python
import asyncio
from aiogram import Bot

async def send_message(bot, chat_id, text):
    await bot.send_message(chat_id, text)

async def main():
    bot = Bot(token="YOUR_TOKEN")

    # 100 параллельных запросов
    tasks = [
        send_message(bot, CHAT_ID, f"Test {i}")
        for i in range(100)
    ]

    await asyncio.gather(*tasks)
```

### Проверка утечек памяти

```bash
# Запустить бота
python main.py &
PID=$!

# Мониторить память
watch -n 1 "ps aux | grep $PID | grep -v grep"

# Отправить 1000 запросов через бота

# Проверить что память не растет бесконечно
```

## Мониторинг логов

### Просмотр логов в реальном времени

```bash
# Основной бот
python main.py 2>&1 | jq '.'

# Админ бот
python admin_main.py 2>&1 | jq '.'
```

### Фильтрация важных событий

```bash
# Только ошибки
python main.py 2>&1 | jq 'select(.level == "error")'

# Только AI запросы
python main.py 2>&1 | jq 'select(.event | contains("ai_request"))'

# Только mock запросы
python main.py 2>&1 | jq 'select(.is_mock == true)'
```

## Production-Ready Checklist

- [x] Бот запускается без ошибок
- [x] Все кнопки работают
- [x] AI запросы обрабатываются (mock режим)
- [x] Токены корректно списываются
- [x] Профиль отображается без ошибок
- [x] Админ-панель работает
- [x] Нет deprecation warnings
- [x] Логи структурированные (JSON)
- [x] Тесты проходят
- [ ] API ключи настроены (опционально)
- [ ] Промокоды работают (требует доработки)
- [ ] Платежи работают (требует yookassa)
- [ ] Production .env настроен

## Переход на production

### 1. Настройка API ключей

```bash
# .env
OPENAI_API_KEY=sk-proj-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_AI_API_KEY=...
DEEPSEEK_API_KEY=...
```

### 2. Отключение mock режима

Mock режим отключается автоматически при наличии API ключей.

Проверка:
```bash
# Логи должны показывать:
{"event": "ai_request_completed", "is_mock": false}
```

### 3. Настройка платежей

```bash
# .env
YUKASSA_SHOP_ID=...
YUKASSA_SECRET_KEY=...
```

### 4. Production окружение

```bash
# .env
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO
```

## Известные ограничения

1. **Промокоды** - интерфейс есть, backend требует доработки
2. **Платежи** - требуется установка yookassa и настройка
3. **Фото/Видео** - UI готов, backend требует реализации
4. **История диалогов** - сохраняется в БД, UI требует доработки

## Следующие шаги

См. `docs/AI_MODELS.md` для детальной информации по AI интеграции.
